%h3 添加新产品
- form_for @product do |f|
  %div
    = label :category, :name, "产品类别"
    = collection_select(:category, :name, ProductCategory.all, :id, :name, {:prompt => "请选择产品类别"})
    = link_to "增加产品类别", new_category_path
  %div
    = f.label :brand, "品牌"
    = f.text_field :brand
  %div
    = f.label :mode, "型号"
    = f.text_field :mode
  %div
    = f.label :detail, "明细"
    = f.text_area :detail
  .action
    %input.submit{ :type => "submit", :value => "保存" }
